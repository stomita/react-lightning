({
	doInit: function(cmp, event, helper) {
		window.ReactLightningRendererDemo.catalog.init(cmp);
	},
	handleEvent: function(cmp, event) {
		window.ReactLightningRendererDemo.catalog.handleEvent(cmp, event);
	}
})
