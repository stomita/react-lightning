<aura:component implements="flexipage:availableForAllPageTypes"
	access="global"
>
	<ltng:require scripts="{!$Resource.ReactLightningBridge}"
		afterScriptsLoaded="{!c.doInit}"
	/>
	{!v.body}
</aura:component>
