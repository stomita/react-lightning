({
	doInit: function(cmp, event, helper) {
		window.ReactLightningRendererDemo.simple.init(cmp);
	},
	handleEvent: function(cmp, event) {
		window.ReactLightningRendererDemo.simple.handleEvent(cmp, event);
	}
})
